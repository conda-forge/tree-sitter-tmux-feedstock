schema_version: 1

context:
  version: "0.1.0"

package:
  name: tree-sitter-tmux
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/t/tree-sitter-tmux/tree_sitter_tmux-${{ version }}.tar.gz
  sha256: 8c2a0781e6145753ed6cfbe152ceaecc2d5c7aa89195a61ef1d7b49532e7b725

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
        - python
  host:
    - python
    - setuptools >=42
    - wheel
    - pip
  run:
    - python

tests:
  - python:
      imports:
        - tree_sitter_tmux
  - requirements:
      run:
        - pip
        - python
    script:
      - pip check

about:
  summary: Tmux grammar for tree-sitter
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/Freed-Wu/tree-sitter-tmux

extra:
  recipe-maintainers:
    - danielnachun
